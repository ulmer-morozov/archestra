import { OAuthProvider } from './base.js';

export class GoogleOAuthProvider extends OAuthProvider {
  // Google OAuth works with the base implementation
  // No additional customization needed for standard PKCE flow
}